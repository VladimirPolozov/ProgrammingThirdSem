<UserControl x:Class="ProgrammingThirdSem.Ales.Views.AlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ProgrammingThirdSem.Ales.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:AlesViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid Margin="24px 12px">
        <StackPanel Orientation="Vertical">
            <StackPanel VerticalAlignment="Top" Orientation="Horizontal" Margin="0px 0 0 16">
                <Button Content="Очистить" Command="{Binding ClearCommand}" Margin="0 0 24px 0" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White"/>
                <Button Content="Загрузить из Excel" Command="{Binding LoadFromExcelCommand}" Margin="0 0 24px 0" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White"/>
                <Button Content="Сгенерировать" Command="{Binding GenerateCommand}" Margin="0 0 24px 0" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White"/>
                <Button Content="+ к таблице" Command="{Binding AddToTableCommand}" Margin="0 0 24px 0" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White"/>
                <Button Content="- от таблицы" Command="{Binding RemoveFromTableCommand}" Margin="0 0 24px 0" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White"/>
            </StackPanel>
            <StackPanel Margin="0 0 0 16" Orientation="Horizontal" HorizontalAlignment="Stretch">
                <Button Content="Рассчитать" Command="{Binding CalculateCommand}" FontFamily="Montserrat" FontSize="11px" FontWeight="600" BorderThickness="0" Background="White" HorizontalAlignment="Left" Margin="0 0 12 0"/>
                <CheckBox Content="Методом Гаусса" FontFamily="Montserrat" FontSize="12px" IsChecked="{Binding IsGaussMethodChecked, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 12 0"/>
                <CheckBox Content="Методом Гаусса-Жордана" FontFamily="Montserrat" FontSize="12px" IsChecked="{Binding IsJordanGaussMethodChecked, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 12 0"/>
                <CheckBox Content="Методом Крамера" FontFamily="Montserrat" FontSize="12px" IsChecked="{Binding IsKramerMethodChecked, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <Grid Margin="0 0 0 12" >
                <DataGrid x:Name="DataGridAles" ItemsSource="{Binding DataView}" AutoGenerateColumns="True" CanUserAddRows="False" RowHeight="20" ColumnWidth="40" RowHeaderWidth="0" />
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" FontSize="12" FontFamily="Montserrat" Text="{Binding GaussCalculationResult, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Grid.Column="1" FontSize="12" FontFamily="Montserrat" Text="{Binding GaussJordanCalculationResult, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Grid.Column="2" FontSize="12" FontFamily="Montserrat" Text="{Binding KramerCalculationResult, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
